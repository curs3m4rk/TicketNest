# Optional development overrides for docker compose.
# Copy this file to .env only when you want to change the defaults.
POSTGRES_DB=ticketnest
POSTGRES_USER=ticketnest
POSTGRES_PASSWORD=ticketnest_Admin
JWT_SECRET=ticketnest-local-development-jwt-secret-change-before-production
TICKETNEST_POSTGRES_PORT=5432
TICKETNEST_APP_PORT=8080
